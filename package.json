{
  "name": "uksa-software-data-ai-training",
  "private": true,
  "scripts": {
    "check:docs": "python scripts/check_tracked_noise.py && python scripts/check_missing_assets.py && python scripts/check_notebook_kernels.py && python scripts/check_notebook_metadata.py",
    "build:docs": "myst build --html"
  },
  "devDependencies": {
    "mystmd": "1.10.1"
  }
}
